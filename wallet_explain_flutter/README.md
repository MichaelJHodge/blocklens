# Blocklens

Flutter frontend for the Blocklens MVP.

## Run

```bash
flutter pub get
flutter run --dart-define=API_BASE_URL=http://localhost:8082
```

## Features

- Wallet input + Ethereum address validation
- Full-screen animated loading state
- Transaction results with status + direction color coding
- Empty state and retry flow for failures
